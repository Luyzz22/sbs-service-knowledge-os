# Production runtime. Keep upper bounds to avoid unreviewed major upgrades.
streamlit==1.62.0
# Streamlit declares watchdog only outside macOS; pin it directly so the
# hash-locked runtime remains reproducible on the Linux image and CI runners.
watchdog==6.0.0
openai==3.6.0
azure-identity==1.25.3
httpx==0.28.1
psycopg[binary]==3.3.5
argon2-cffi==25.1.0
