# Nur für die lokale Entwicklung. Produktion verwendet Entra ID, Managed Identity
# und Azure Key Vault; dort werden keine Provider-API-Schlüssel hinterlegt.
LOCAL_USERS_JSON = '{"local-admin":{"display_name":"Local Admin","password_hash":"$argon2id$REPLACE_ME","role":"admin"}}'
AUDIT_HMAC_KEY = "replace-with-at-least-32-random-bytes"
